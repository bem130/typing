<Window x:Class="typing.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:typing"
        mc:Ignorable="d"
        Title="typing - log" Height="350" Width="500"
        PreviewKeyDown="Window_PreviewKeyDown"
        Closing="LogWindow_Closing"
        Background="{DynamicResource cBackground}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="elements.xaml"/>
                <ResourceDictionary Source="colorthemes/default.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>

        <ListView Grid.Row="1" x:Name="Logs" Foreground="{DynamicResource cFontcolor}" Background="{DynamicResource cBackground}" ItemContainerStyle="{DynamicResource ListViewItemStyle}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="status" DisplayMemberBinding="{Binding status}" Width="35"></GridViewColumn>
                    <GridViewColumn Header="datetime" DisplayMemberBinding="{Binding datetime}" Width="110"></GridViewColumn>
                    <GridViewColumn Header="contents" DisplayMemberBinding="{Binding contents}" Width="320"></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        
        
    </Grid>
</Window>
